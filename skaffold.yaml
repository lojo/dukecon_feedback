apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/dukecon-jx/dukecon-feedback:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/dukecon-jx/dukecon-feedback:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: dukecon-feedback
        chartPath: charts/dukecon-feedback
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/dukecon-jx/dukecon-feedback'
          image.tag: '{{.DIGEST_HEX}}'
